
public class HW3_WrittenPortion {
/*
	1.Assume we have the following variable declarations:
	String b = "Bungalow Bill";
	String m = "Mean Mr. Mustard";
	String r = "Rocky Raccoon";
	Determine what the following expressions evaluate to. If any of them raise an exception,
	write “Exception”. Use a to represent any blanks in your answers.
	a) b.length() 		f) r.indexOf("R") 		k) b.substring(3)
	b) m.charAt(6) 		g) m.indexOf("R") 		l) b.substring(7,7)
	c) r.charAt(13) 	h) r.indexOf("R", 2) 	m) m.substring(5,8)+b.substring(8)
	d) (r+m).length() 	i) m.lastIndexOf("M") 	n) r.charAt(r.length()/2)
	e) r+m.length() 	j) m.substring(3,7) 	o) b.charAt(b.charAt(9)-’A’)

	2.Suppose we have a String variable name which contains someone’s last name using
	only lower case letters (like "bonomo"). Write a statement that would have name store the
	same name but with the first letter capitalized (like "Bonomo"). You should make use of
	toUpperCase and other methods in the String class.

	name = name.substring(0, 1).toUpperCase() + name.substring(1);
*/
}
